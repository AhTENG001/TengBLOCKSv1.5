# TengBLOCKSv1.5
TENGBlocks is an advanced Python Block-based Editor that allows you to do anything that normal Python can! You can import modules, create custom blocks, and even install modules from command prompt! *Note: Only works on Windows, unless you do special setups to run .exe files on Linux and MacOS. as it is packaged into an .EXE file
